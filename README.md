# Telegram-бот "ТЦОС Квиз" + Mini App

Проект состоит из двух частей:

1. **Python Бот**: Обрабатывает команды и может запускать тесты прямо в чате.
2. **Mini App (React)**: Интерактивный интерфейс для прохождения квизов и админ-панель.

## 🚀 Развертывание Mini App (Frontend)

Telegram требует, чтобы Mini App работал по протоколу **HTTPS**.

### Вариант 1: Vercel (Самый простой)

1. Создайте аккаунт на [Vercel.com](https://vercel.com).
2. Установите Vercel CLI: `npm install -g vercel`.
3. В корне проекта (где лежит `index.html` и `App.tsx`) выполните команду: `vercel`.
4. Следуйте инструкциям. После загрузки вы получите ссылку (например, `https://tcos-quiz.vercel.app`).
5. **Эту ссылку нужно вставить в `bot.py` (переменная `WEBAPP_URL`)**.

### Вариант 2: GitHub Pages

1. Загрузите проект в репозиторий на GitHub.
2. В настройках репозитория (**Settings -> Pages**) выберите источником ветку `main` или используйте GitHub Actions для сборки Vite.
3. После публикации вы получите ссылку `https://username.github.io/repo-name/`.

---

## 🤖 Запуск Бота (Backend)

1. Перейдите в папку `tcos_quiz_bot`.
2. Настройте `.env` (вставьте токен от @BotFather).
3. **ОБЯЗАТЕЛЬНО**: В файле `bot.py` обновите `WEBAPP_URL` на вашу ссылку из шага развертывания Frontend.
4. Запустите бота: `python bot.py`.

---

## 🛠 Использование Админ-панели

Поскольку в данном примере не используется внешняя база данных (для простоты запуска), админ-панель работает по принципу "Экспорта":

1. Откройте Mini App в Telegram.
2. Перейдите в режим **"Админ"** (кнопка в углу).
3. Добавьте нужные темы и вопросы.
4. Внизу панели появится блок **"Экспорт данных"**. Скопируйте этот JSON-текст.
5. Замените содержимое словаря `QUIZ_DATA` в файле `quiz_data.py` на этот текст.
6. Перезапустите Python-бота.

## Решение проблем

- **Button_data_invalid**: Ошибка исправлена использованием индексов `t:0`, `a:0` вместо длинных строк.
- **Приложение не открывается**: Проверьте, что в `WEBAPP_URL` указана ссылка с `https://`.
